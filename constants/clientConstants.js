import { DEFAULT_HOST } from "./globalConstants";

export const HTTP_METHODS = {
  POST: "POST",
  GET: "GET",
};

export const RESPONSE_STATUSES = {
  OK: "OK",
  ERROR: "ERROR",
};

export const MAX_SALES_ORDERS = 10;
